import { useState } from "react";
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from "react-native";
import { Bus, Clock, Wallet, Zap } from "lucide-react-native";
import Card from "../components/Card";
import { api } from "../lib/api";
import { colors } from "../theme/colors";

const destinations = ["Hundred Islands", "Patar Beach", "Manaoag Shrine", "Lingayen", "Bolinao", "Urdaneta"];
const preferences = ["Budget-friendly", "Island hopping", "Cultural sites", "Food stops", "Photography spots", "Accessible routes"];
const features = [[Clock, colors.oceanBlueLight, colors.oceanBlue, "Smart Timing", "AI picks optimal departure times to avoid traffic and peak hours"], [Wallet, colors.palmGreenLight, colors.palmGreen, "Budget-Aware", "Each stop is balanced to keep your total spend within budget"], [Bus, colors.coralLight, colors.sunsetCoral, "Transit-Smart", "Routes use real Pangasinan jeepney and bus schedules"]];

export default function AIItinerary() { const [destination, setDestination] = useState(destinations[0]); const [budget, setBudget] = useState("2000"); const [days, setDays] = useState(1); const [prefs, setPrefs] = useState([]); const [loading, setLoading] = useState(false); const [itinerary, setItinerary] = useState(null); const [error, setError] = useState(null); const toggle = (value) => setPrefs((current) => current.includes(value) ? current.filter((item) => item !== value) : [...current, value]); const generate = async () => { setLoading(true); setError(null); setItinerary(null); try { setItinerary(await api.generateItinerary({ destination, budget, days, preferences: prefs })); } catch (cause) { setError(cause instanceof Error ? cause.message : "Failed to generate itinerary."); } finally { setLoading(false); } }; const total = itinerary?.days?.reduce((all, day) => all + day.stops.reduce((sum, stop) => sum + (stop.estimatedCost || 0), 0), 0) || 0; return <ScrollView contentContainerStyle={styles.screen}><View style={styles.header}><Text style={styles.title}>AI Itinerary Planner</Text><Text style={styles.sub}>Smart trip planning powered by Claude AI</Text></View><Card style={styles.card}><Text style={styles.heading}>Where do you want to go?</Text><Text style={styles.label}>Destination</Text><View style={styles.destinationGrid}>{destinations.map((item) => <Pressable key={item} onPress={() => setDestination(item)} style={[styles.destination, destination === item && styles.destinationActive]}><Text style={[styles.destinationText, destination === item && styles.destinationTextActive]}>{item}</Text></Pressable>)}</View><View style={styles.inputs}><View style={styles.inputGroup}><Text style={styles.label}>Trip Budget (₱)</Text><TextInput value={budget} onChangeText={setBudget} keyboardType="numeric" style={styles.input} /></View><View style={styles.inputGroup}><Text style={styles.label}>Number of Days</Text><View style={styles.dayButtons}>{[1, 2, 3, 4, 5, 6, 7].map((value) => <Pressable key={value} onPress={() => setDays(value)} style={[styles.day, value === days && styles.dayActive]}><Text style={[styles.dayText, value === days && styles.dayTextActive]}>{value}</Text></Pressable>)}</View></View></View><Text style={styles.label}>Travel Preferences</Text><View style={styles.chips}>{preferences.map((item) => <Pressable key={item} onPress={() => toggle(item)} style={[styles.chip, prefs.includes(item) && styles.chipActive]}><Text style={[styles.chipText, prefs.includes(item) && styles.chipTextActive]}>{item}</Text></Pressable>)}</View><Pressable disabled={loading} onPress={generate} style={[styles.generate, loading && styles.disabled]}>{loading ? <ActivityIndicator color={colors.white} /> : <Zap size={18} color={colors.white} />}<Text style={styles.generateText}>{loading ? "Generating your itinerary..." : "Generate AI Itinerary"}</Text></Pressable>{error ? <Text style={styles.error}>{error}</Text> : null}</Card>{itinerary ? <Card style={styles.card}><Text style={styles.heading}>Your {days}-Day {destination} Itinerary</Text>{itinerary.days?.map((plan) => <View key={plan.day} style={styles.dayPlan}><View style={styles.planTitle}><Text style={styles.dayNumber}>{plan.day}</Text><Text style={styles.planHeading}>Day {plan.day}</Text></View>{plan.stops.map((stop, index) => <View key={`${stop.time}-${index}`} style={[styles.stop, index < plan.stops.length - 1 && styles.stopBorder]}><Text style={styles.time}>{stop.time}</Text><View style={styles.stopDetails}><Text style={styles.place}>{stop.place}</Text><Text style={styles.sub}>{stop.activity}</Text></View><Text style={styles.cost}>₱{stop.estimatedCost?.toLocaleString() || "—"}</Text></View>)}<Text style={styles.dayTotal}>Day total: ₱{plan.stops.reduce((sum, stop) => sum + (stop.estimatedCost || 0), 0).toLocaleString()}</Text></View>)}<View style={styles.grandTotal}><Text style={styles.grandLabel}>Total Estimated Cost</Text><Text style={styles.grandCost}>₱{total.toLocaleString()}</Text></View></Card> : null}<View style={styles.features}>{features.map(([Icon, backgroundColor, iconColor, name, description]) => <Card key={name} style={styles.feature}><View style={[styles.featureIcon, { backgroundColor }]}><Icon size={18} color={iconColor} /></View><Text style={styles.featureName}>{name}</Text><Text style={styles.sub}>{description}</Text></Card>)}</View></ScrollView>; }
const styles = StyleSheet.create({ screen: { flexGrow: 1, padding: 28, backgroundColor: "#F7F9FB" }, header: { marginBottom: 28 }, title: { fontFamily: "Poppins", fontSize: 26, fontWeight: "700", color: colors.textPrimary }, sub: { marginTop: 4, fontFamily: "DMSans", fontSize: 13, color: colors.textMuted }, card: { marginBottom: 24, padding: 28 }, heading: { marginBottom: 24, fontFamily: "Poppins", fontSize: 20, fontWeight: "700", color: colors.textPrimary }, label: { marginBottom: 10, fontFamily: "DMSans", fontSize: 13, fontWeight: "500", color: colors.textMuted }, destinationGrid: { flexDirection: "row", flexWrap: "wrap", gap: 10, marginBottom: 24 }, destination: { width: "31%", minWidth: 170, padding: 12, borderWidth: 1, borderColor: colors.border, borderRadius: 10 }, destinationActive: { borderColor: colors.oceanBlue, backgroundColor: colors.oceanBlue }, destinationText: { fontFamily: "DMSans", fontSize: 14, color: colors.textPrimary }, destinationTextActive: { color: colors.white, fontWeight: "600" }, inputs: { flexDirection: "row", gap: 20, marginBottom: 24 }, inputGroup: { flex: 1 }, input: { paddingHorizontal: 14, paddingVertical: 12, borderWidth: 1, borderColor: colors.border, borderRadius: 10, backgroundColor: colors.white, fontFamily: "DMSans", color: colors.textPrimary }, dayButtons: { flexDirection: "row", flexWrap: "wrap", gap: 6 }, day: { width: 34, height: 42, alignItems: "center", justifyContent: "center", borderWidth: 1, borderColor: colors.border, borderRadius: 8 }, dayActive: { backgroundColor: colors.oceanBlue, borderColor: colors.oceanBlue }, dayText: { fontFamily: "DMSans", color: colors.textPrimary }, dayTextActive: { color: colors.white, fontWeight: "700" }, chips: { flexDirection: "row", flexWrap: "wrap", gap: 8, marginBottom: 32 }, chip: { paddingHorizontal: 16, paddingVertical: 8, borderWidth: 1, borderColor: "#D6E4EF", borderRadius: 20 }, chipActive: { borderColor: colors.oceanBlue, backgroundColor: colors.oceanBlueLight }, chipText: { fontFamily: "DMSans", fontSize: 13, color: colors.textMuted }, chipTextActive: { color: colors.oceanBlue, fontWeight: "600" }, generate: { flexDirection: "row", alignItems: "center", justifyContent: "center", gap: 8, padding: 16, borderRadius: 12, backgroundColor: colors.sunsetCoral }, disabled: { backgroundColor: "#B0C4D4" }, generateText: { fontFamily: "DMSans", fontSize: 15, fontWeight: "700", color: colors.white }, error: { marginTop: 16, padding: 12, borderRadius: 10, backgroundColor: colors.coralLight, fontFamily: "DMSans", fontSize: 13, color: colors.sunsetCoral }, dayPlan: { marginBottom: 28 }, planTitle: { flexDirection: "row", alignItems: "center", gap: 12, marginBottom: 12 }, dayNumber: { width: 36, height: 36, borderRadius: 18, paddingTop: 8, textAlign: "center", backgroundColor: colors.oceanBlue, fontFamily: "Poppins", fontWeight: "700", color: colors.white }, planHeading: { fontFamily: "Poppins", fontSize: 16, fontWeight: "700", color: colors.textPrimary }, stop: { flexDirection: "row", gap: 16, paddingVertical: 16 }, stopBorder: { borderBottomWidth: 1, borderBottomColor: "#F0F5FA" }, time: { width: 70, alignSelf: "flex-start", paddingHorizontal: 8, paddingVertical: 3, borderRadius: 6, backgroundColor: colors.coralLight, fontFamily: "DMSans", fontSize: 12, fontWeight: "600", color: colors.sunsetCoral }, stopDetails: { flex: 1 }, place: { fontFamily: "DMSans", fontSize: 14, fontWeight: "700", color: colors.textPrimary }, cost: { fontFamily: "DMSans", fontSize: 13, fontWeight: "700", color: colors.palmGreen }, dayTotal: { alignSelf: "flex-end", marginTop: 12, fontFamily: "DMSans", fontSize: 14, fontWeight: "700", color: colors.oceanBlue }, grandTotal: { flexDirection: "row", justifyContent: "space-between", padding: 16, borderRadius: 12, backgroundColor: colors.oceanBlueLight }, grandLabel: { fontFamily: "Poppins", fontSize: 15, fontWeight: "700", color: colors.oceanBlue }, grandCost: { fontFamily: "Poppins", fontSize: 18, fontWeight: "700", color: colors.sunsetCoral }, features: { flexDirection: "row", gap: 16 }, feature: { flex: 1 }, featureIcon: { width: 40, height: 40, borderRadius: 20, alignItems: "center", justifyContent: "center", marginBottom: 12 }, featureName: { fontFamily: "Poppins", fontSize: 14, fontWeight: "600", color: colors.textPrimary } });
