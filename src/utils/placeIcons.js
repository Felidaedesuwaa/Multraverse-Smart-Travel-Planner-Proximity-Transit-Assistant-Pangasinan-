import { Waves, Trees, Anchor, Landmark, Church, UtensilsCrossed } from "lucide-react-native";

// Single source of truth for the icon used per place/trip category.
// Reuses the same icon vocabulary as PangasinanMap's iconMap, plus "utensils" for food spots.
export const placeIconMap = {
  waves: Waves,
  trees: Trees,
  anchor: Anchor,
  landmark: Landmark,
  church: Church,
  utensils: UtensilsCrossed,
};

